# sagax-lora

**LoRA + QLoRA from scratch** — 4-bit NormalFloat4, double quantization, paged optimizers.  
Zero external dependencies. PEFT and bitsandbytes are not used.

**LoRA workflow**
![LoRA workflow]("sagax-lora\assets\LoRA_algo_flow.png")

**QLoRA workflow**
![QLoRA workflow]("sagax-lora\assets\QLoRA_algo_flow.png")
